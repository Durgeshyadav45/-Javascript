// Qus1: A "good string" is a string that starts with letter 'a' & has a length>3.Write a Program to find if a string is good or not

//QS: predict output of follwing code
  
let str = "apple";

if (str[0] === "a" && str.length > 3){
    console.log("good string")
}
else{
    console.log("not good string")
}